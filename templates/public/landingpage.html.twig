{% extends 'base.html.twig' %}

{% block title %}OSA Cockpit - Sportabzeichen-Verwaltung für Schulen & Vereine{% endblock %}

{% block layout %}

{# Public Navbar #}
<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top px-4">
    <a class="navbar-brand fw-bold text-success" href="#">
        <i class="fas fa-medal me-2"></i>OSA Cockpit
    </a>
    <div class="ms-auto d-flex align-items-center gap-3">
        <a href="{{ path('app_login') }}" class="text-decoration-none text-secondary fw-bold">Login</a>
        <a href="{{ path('app_register') }}" class="btn btn-success rounded-pill px-4 shadow-sm">Jetzt starten</a>
    </div>
</nav>

<div class="public-wrapper" style="background-color: #f4f6f9; min-height: 100vh;">
    {# Hero Section #}
    <section class="hero-section py-5 text-center">
        <div class="container py-5 mt-4">
            <span class="badge rounded-pill bg-success bg-opacity-10 text-success mb-3 px-3 py-2 text-uppercase fw-bold border border-success">
                Für Schulen & Sportvereine
            </span>
            <h1 class="display-4 fw-bold mb-4 text-dark">Das digitale Cockpit für <br>dein <span class="text-success">Sportabzeichen</span></h1>
            <p class="lead text-secondary mx-auto mb-5" style="max-width: 650px;">
                Egal ob Bundesjugendspiele, Vereinsfest oder regulärer Sportunterricht: Erfasse Leistungen direkt auf dem Platz, verwalte Teilnehmer mühelos und lass das System die Medaillen berechnen.
            </p>
            <div class="d-flex justify-content-center gap-3">
                <a href="{{ path('app_register') }}" class="btn btn-success btn-lg px-5 rounded-pill shadow">Kostenlos registrieren</a>
            </div>
        </div>
    </section>

    {# Feature Bento Grid #}
    <section id="features" class="py-5 container">
        <div class="row g-4">
            
            {# Feature 1: Leistungserfassung (exam_results) #}
            <div class="col-md-8">
                <div class="card h-100 border-0 shadow-sm overflow-hidden" style="background-color: #2c3e50;">
                    <div class="card-body p-4 p-md-5 d-flex flex-column">
                        <div class="mb-4">
                            <span class="badge bg-primary mb-2">Live auf dem Platz</span>
                            <h3 class="fw-bold text-white">Blitzschnelle Leistungserfassung</h3>
                            <p class="text-white-50">Tippe Zeiten und Weiten direkt am Smartphone ein. Kein lästiges Umrechnen mehr – das System weiß sofort, ob es für Gold, Silber oder Bronze reicht.</p>
                        </div>
                        
                        {# --- START RESULT INPUT MOCKUP --- #}
                        <div class="mt-auto bg-light rounded-3 p-3 shadow-lg transform-mockup">
                            <div class="d-flex justify-content-between align-items-center mb-3 pb-2 border-bottom">
                                <span class="fw-bold text-dark"><i class="fas fa-running me-2"></i>50m Sprint (Altersklasse M12)</span>
                            </div>
                            
                            {# Zeile 1 #}
                            <div class="d-flex align-items-center bg-white p-2 rounded border mb-2 shadow-sm">
                                <div class="me-auto fw-bold text-dark">Leon M.</div>
                                <div class="input-group input-group-sm w-25 me-3">
                                    <input type="text" class="form-control text-end fw-bold text-success" value="7,4" readonly>
                                    <span class="input-group-text bg-light">sek</span>
                                </div>
                                <div class="medal-circle-sm bg-gold">G</div>
                            </div>
                            
                            {# Zeile 2 #}
                            <div class="d-flex align-items-center bg-white p-2 rounded border shadow-sm">
                                <div class="me-auto fw-bold text-dark">Jonas K.</div>
                                <div class="input-group input-group-sm w-25 me-3">
                                    <input type="text" class="form-control text-end" placeholder="z.B. 8,1" readonly>
                                    <span class="input-group-text bg-light">sek</span>
                                </div>
                                <div class="medal-circle-sm bg-secondary text-white opacity-50">-</div>
                            </div>
                        </div>
                        {# --- END RESULT INPUT MOCKUP --- #}
                    </div>
                </div>
            </div>

            {# Feature 2: Teilnehmer & Listen #}
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm p-4 bg-white">
                    <div class="card-body d-flex flex-column">
                        <div class="text-primary mb-3"><i class="fas fa-users-cog fa-2x"></i></div>
                        <h4 class="fw-bold">Teilnehmer- & Listenverwaltung</h4>
                        <p class="text-muted small mb-4">Importiere Klassenlisten oder Vereinsgruppen mit wenigen Klicks. Weise Sportlehrer oder Prüfer direkt den jeweiligen Stationen zu.</p>
                        
                        {# --- START ADMIN MOCKUP --- #}
                        <div class="mt-auto border rounded p-3 bg-light">
                            <div class="d-flex align-items-center justify-content-between mb-2">
                                <span class="small fw-bold text-secondary">Klasse 7b</span>
                                <span class="badge bg-success bg-opacity-10 text-success border border-success">24 Schüler</span>
                            </div>
                            <div class="d-flex align-items-center justify-content-between">
                                <span class="small fw-bold text-secondary">Prüfer</span>
                                <span class="small bg-white border px-2 py-1 rounded shadow-sm"><i class="fas fa-user-check text-primary me-1"></i> Herr Müller</span>
                            </div>
                        </div>
                        {# --- END ADMIN MOCKUP --- #}
                    </div>
                </div>
            </div>

            {# Feature 3: Medaillen & Urkunden #}
            <div class="col-md-7">
                <div class="card h-100 border-0 shadow-sm p-4 p-md-5 bg-white">
                    <div class="card-body d-flex flex-column justify-content-center text-center">
                        <h3 class="fw-bold">Automatische Auswertung</h3>
                        <p class="text-muted mb-4 mx-auto" style="max-width: 400px;">Sobald alle vier Disziplinen erfüllt sind, berechnet das System die Gesamtpunktzahl und bereitet die Daten für die Urkunden vor.</p>
                        
                        <div class="d-flex justify-content-center gap-4 mt-2">
                            <div class="text-center">
                                <div class="medal-circle bg-gold shadow-sm mb-2 mx-auto">G</div>
                                <small class="fw-bold text-dark">Gold</small>
                            </div>
                            <div class="text-center">
                                <div class="medal-circle bg-silver shadow-sm mb-2 mx-auto">S</div>
                                <small class="fw-bold text-dark">Silber</small>
                            </div>
                            <div class="text-center">
                                <div class="medal-circle bg-bronze shadow-sm mb-2 mx-auto">B</div>
                                <small class="fw-bold text-dark">Bronze</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {# Feature 4: Eduplaces Integration (COMING SOON) #}
            <div class="col-md-5">
                <div class="card h-100 border-0 shadow-sm overflow-hidden" style="background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);">
                    <div class="card-body p-4 position-relative">
                        <span class="position-absolute top-0 end-0 bg-primary text-white px-3 py-1 rounded-bottom-start shadow-sm small fw-bold">
                            Zukunftssicher
                        </span>
                        <div class="mt-3 mb-3 text-primary"><i class="fas fa-university fa-2x"></i></div>
                        <h4 class="fw-bold text-dark">Eduplaces Integration</h4>
                        <p class="text-dark opacity-75 small">
                            Wir arbeiten an einer nahtlosen Anbindung an das <strong>Eduplaces</strong> Ökosystem. 
                            Bald können sich Lehrkräfte direkt mit ihrem Schul-Account einloggen und Klassenlisten synchronisieren.
                        </p>
                        
                        <div class="mt-4 bg-white p-2 rounded shadow-sm d-flex align-items-center opacity-75">
                            <i class="fas fa-sync fa-spin text-primary me-3 ms-2"></i>
                            <span class="small fw-bold text-secondary">Single Sign-On in Vorbereitung...</span>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </section>

    {# Call to Action Footer #}
    <section class="py-5 bg-white border-top mt-5">
        <div class="container text-center py-4">
            <h2 class="fw-bold mb-3">Zeit, den Papierkram zu beenden.</h2>
            <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg px-5 mt-2 rounded-pill shadow-sm">Jetzt Verein oder Schule registrieren</a>
        </div>
    </section>
</div>

<style>
    /* Spezifisches Styling für die Mockups */
    .medal-circle {
        width: 60px; height: 60px; border-radius: 50%;
        display: flex; align-items: center; justify-content: center;
        font-size: 1.5rem; font-weight: bold; color: white;
    }
    .medal-circle-sm {
        width: 32px; height: 32px; border-radius: 50%;
        display: flex; align-items: center; justify-content: center;
        font-size: 0.9rem; font-weight: bold; color: white;
    }
    
    .bg-gold { background: linear-gradient(135deg, #FFD700, #DAA520); }
    .bg-silver { background: linear-gradient(135deg, #E0E0E0, #9E9E9E); }
    .bg-bronze { background: linear-gradient(135deg, #CD7F32, #8D6E63); }
    
    .transform-mockup {
        transform: perspective(1000px) rotateX(2deg) rotateY(-2deg) translateY(5px);
        transition: transform 0.4s ease, box-shadow 0.4s ease;
    }
    .transform-mockup:hover {
        transform: perspective(1000px) rotateX(0deg) rotateY(0deg) translateY(0px);
        box-shadow: 0 1rem 3rem rgba(0,0,0,.175)!important;
    }
    
    /* Abrundung der Buttons */
    .rounded-pill { border-radius: 50rem !important; }
</style>
{% endblock %}