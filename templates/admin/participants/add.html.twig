{% extends 'base.html.twig' %}

{% block page_title %}{{ _('Teilnehmerdaten ergänzen') }}{% endblock %}

{% block content %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">Daten für {{ user.firstname }} {{ user.lastname }}</h3>
        </div>
        <div class="panel-body">
            {{ form_start(form) }}
                {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}