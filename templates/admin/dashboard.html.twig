{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1 class="h3 mb-4 text-gray-800">Verwaltung</h1>

        {# Tabs einbinden #}
        {% include 'admin/_tabs.html.twig' with {'activeTab': activeTab} %}

        <div class="card shadow mb-4">
            <div class="card-body">
                <h4>Willkommen im Administrationsbereich</h4>
                <p>Hier können Teilnehmer importiert, Gruppen verwaltet und die Anforderungen für das Sportabzeichen aktualisiert werden.</p>
                
                <div class="row mt-4">
                    <div class="col-md-4">
                        <div class="alert alert-info">
                            <h5><i class="fas fa-database"></i> Anforderungen</h5>
                            <p>Importiere die aktuelle Tabelle des DOSB.</p>
                            <a href="{{ path('admin_requirements_upload') }}" class="btn btn-sm btn-primary">Zum Upload</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}