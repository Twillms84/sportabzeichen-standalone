<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Sportabzeichen App{% endblock %}</title>
        
        {# Bootstrap 5 & FontAwesome via CDN laden (Schnellste Lösung für Styles & Icons) #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {# Einfache Navbar (Ersatz für das IServ-Menü) #}
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
            <div class="container">
                <a class="navbar-brand" href="#">Sportabzeichen</a>
                <div class="navbar-nav">
                     {# Hier später deine Links rein #}
                    <a class="nav-link" href="#">Start</a>
                    <a class="nav-link" href="#">Prüfungen</a>
                </div>
            </div>
        </nav>

        <div class="container">
            {# Flash Messages (Erfolgsmeldungen anzeigen) #}
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            {# --- HIER IST DER TRICK --- #}
            {# Wir packen deine alten IServ-Blöcke in den neuen Body #}
            {% block body %}
                <div class="card">
                    <div class="card-header">
                        <h1>{% block headline %}Willkommen{% endblock %}</h1>
                    </div>
                    <div class="card-body">
                        {# Hier wird der Inhalt deiner alten Templates reingeladen #}
                        {% block content %}{% endblock %}
                    </div>
                </div>
            {% endblock %}
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>