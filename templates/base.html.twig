<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Sportabzeichen App{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    {# --- CSS --- #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {# Bootstrap Select lassen wir drin, falls du es für alte Module brauchst, aber wir stylen es um #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.14.0-beta2/css/bootstrap-select.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    {% block stylesheets %}
    <style>
        /* --- 1. FARBPALETTE (Dunkelgrün & Dark Mode) --- */
        :root {
            --osa-bg-dark: #121212;
            --osa-card-bg: #1e1e1e;
            --osa-border: #333;
            /* Dein gewünschtes Dunkelgrün */
            --osa-green-main: #144018; 
            --osa-green-hover: #1b5220;
            --osa-text: #e9ecef;
        }

        body {
            background-color: var(--osa-bg-dark);
            color: var(--osa-text);
        }

        /* --- 2. LAYOUT FIXES (Sidebar Sticky) --- */
        @media (min-width: 992px) {
            body { display: flex; min-height: 100vh; }
            .sidebar-wrapper {
                width: 280px;
                height: 100vh;
                position: sticky;
                top: 0;
                flex-shrink: 0;
                overflow-y: auto;
                border-right: 1px solid var(--osa-border);
            }
            .main-content {
                flex-grow: 1;
                overflow-x: hidden;
                width: 100%; /* Verhindert Umbruch-Probleme */
            }
        }

        /* --- 3. BUTTONS (Dunkelgrün Style) --- */
        /* Wir überschreiben Primary & Success für einheitlichen Look */
        .btn-primary, .btn-success, .btn-osa-primary {
            background-color: var(--osa-green-main) !important;
            border-color: var(--osa-green-main) !important;
            color: #fff !important;
        }
        .btn-primary:hover, .btn-success:hover, .btn-osa-primary:hover {
            background-color: var(--osa-green-hover) !important;
            border-color: var(--osa-green-hover) !important;
        }
        
        /* Outline Buttons für Dark Mode anpassen */
        .btn-outline-secondary {
            color: #adb5bd;
            border-color: #495057;
        }
        .btn-outline-secondary:hover {
            background-color: #343a40;
            color: #fff;
        }

        /* --- 4. NEUE DROP DOWNS (Das Template) --- */
        /* Nutze die Klasse .osa-dropdown-menu für neue Menüs */
        .dropdown-menu, .osa-dropdown-menu {
            background-color: #2b3035 !important; /* Dunkles Grau */
            border: 1px solid #495057 !important;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.5) !important;
            padding: 0.5rem 0;
            border-radius: 0.375rem;
        }

        .dropdown-item {
            color: #dee2e6 !important;
        }

        .dropdown-item:hover, .dropdown-item:focus {
            background-color: var(--osa-green-main) !important; /* Hover wird grün */
            color: #fff !important;
        }

        .dropdown-divider {
            border-top-color: #495057 !important;
        }

        /* Fix für Bootstrap-Select im Dark Mode, falls genutzt */
        .bootstrap-select .dropdown-menu { background-color: #2b3035; }
        .bootstrap-select .dropdown-toggle { background-color: #2b3035; color: #fff; border: 1px solid #495057; }
        
        /* Formularelemente dunkel */
        .form-control, .form-select {
            background-color: #212529;
            border: 1px solid #495057;
            color: #fff;
        }
        .form-control:focus, .form-select:focus {
            background-color: #2b3035;
            border-color: var(--osa-green-main);
            color: #fff;
            box-shadow: 0 0 0 0.25rem rgba(20, 64, 24, 0.25);
        }
    </style>
    {% endblock %}

    {% block javascripts %}
        {# Zuerst jQuery (falls benötigt) #}
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        {# Dann Bootstrap Bundle (WICHTIG für Dropdowns) #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {# Dann Bootstrap Select #}
        <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.14.0-beta2/js/bootstrap-select.min.js"></script>
        
        {{ importmap('app') }}

        <script>
            // Initialisierung aller Dropdowns sicherstellen
            document.addEventListener('DOMContentLoaded', function () {
                var dropdownElementList = [].slice.call(document.querySelectorAll('[data-bs-toggle="dropdown"]'))
                var dropdownList = dropdownElementList.map(function (dropdownToggleEl) {
                    return new bootstrap.Dropdown(dropdownToggleEl)
                });
            });
        </script>
    {% endblock %}
</head>
<body>

    {# --- LAYOUT BLOCK START --- #}
    {% block layout %}

        {# 1. MOBILE HEADER #}
        <nav class="navbar navbar-dark bg-dark mobile-header px-3 d-lg-none sticky-top">
            <button class="btn btn-outline-light" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarMenu">
                <i class="fas fa-bars"></i> Menü
            </button>
            <span class="navbar-brand ms-2">Sportabzeichen</span>
        </nav>

        {# 2. SIDEBAR CONTAINER #}
        <div class="offcanvas-lg offcanvas-start sidebar-wrapper bg-dark" tabindex="-1" id="sidebarMenu">
            
            <div class="offcanvas-header d-lg-none border-bottom border-secondary">
                <h5 class="offcanvas-title text-white">Menü</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" data-bs-target="#sidebarMenu" aria-label="Close"></button>
            </div>

            <div class="sidebar d-flex flex-column h-100">
                
                {# Logo #}
                <a href="{{ path('app_exams_dashboard') }}" class="sidebar-brand text-decoration-none text-white p-3 d-block">
                     <i class="fas fa-medal me-2 text-warning"></i> <strong>Sportabzeichen</strong>
                </a>

                <hr class="my-0 border-secondary">

                {# Navigation #}
                <ul class="nav flex-column mb-auto mt-3 px-2">
                    <li class="nav-item mb-1">
                        {% set is_exam = app.request.attributes.get('_route') starts with 'app_exams_' %}
                        {# Hier nutzen wir bg-success (was wir oben auf dunkelgrün umgebogen haben) #}
                        <a href="{{ path('app_exams_dashboard') }}" 
                           class="nav-link text-white {% if is_exam %}active bg-success{% endif %} rounded">
                            <i class="fas fa-tachometer-alt me-2"></i> Dashboard
                        </a>
                    </li>
                    
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item mb-1">
                        {% set is_admin = app.request.attributes.get('_route') starts with 'admin_' %}
                        <a href="{{ path('admin_dashboard') }}" class="nav-link text-white opacity-75 {% if is_admin %}active bg-success{% endif %} rounded">
                            <i class="fas fa-tools me-2"></i> Verwaltung
                        </a>
                    </li>
                    {% endif %}
                </ul>
                
                {# Footer #}
                <div class="mt-auto p-3 border-top border-secondary bg-black bg-opacity-25">
                    <div class="d-flex align-items-center justify-content-between">
                        <div class="d-flex align-items-center overflow-hidden">
                            <i class="fas fa-user-circle fs-4 text-secondary me-2"></i>
                            <div class="lh-1 text-truncate" style="max-width: 130px;">
                                <span class="d-block text-white-50 small" style="font-size: 0.75rem;">Angemeldet als</span>
                                <span class="text-white fw-bold small text-truncate d-block">
                                    {{ app.user ? (app.user.firstname|default(app.user.username)) : 'Gast' }}
                                </span>
                            </div>
                        </div>
                        <a href="{{ path('app_logout') }}" class="btn btn-sm btn-outline-danger border-0" title="Abmelden">
                            <i class="fas fa-sign-out-alt fs-5"></i>
                        </a>
                    </div>
                </div>

            </div>
        </div>

        {# 3. MAIN CONTENT #}
        <main class="main-content p-4">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </main>

    {% endblock %} 

</body>
</html>