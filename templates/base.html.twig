<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Sportabzeichen App{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    {# --- CSS --- #}
    {# Bootstrap 5 CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    {# Bootstrap-Select CSS #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.14.0-beta2/css/bootstrap-select.min.css">
    
    {# FontAwesome #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    {# WICHTIG: Deine eigenen Styles und JS Logik #}
    {% block stylesheets %}{% endblock %}

    {% block javascripts %}
        {{ importmap('app') }}
    {% endblock %}
</head>
<body>

    {# --- HIER STARTET DER LAYOUT BLOCK --- #}
    {% block layout %}
    
        {# 1. Mobile Header (Bleibt im Layout-Block) #}
        <nav class="navbar navbar-dark bg-dark mobile-header px-3 d-lg-none sticky-top">
            <button class="btn btn-outline-light" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarMenu">
                <i class="fas fa-bars"></i> Menü
            </button>
            <span class="navbar-brand ms-2">Sportabzeichen</span>
        </nav>

        {# 2. Sidebar Wrapper (Bleibt im Layout-Block) #}
        <div class="offcanvas-lg offcanvas-start sidebar-wrapper bg-dark" tabindex="-1" id="sidebarMenu">
            {# ... Dein Sidebar Code hier ... #}
            {# (Ich kürze das hier ab, dein Code bleibt wie er war) #}
             <div class="sidebar d-flex flex-column h-100">
                {# ... Inhalt der Sidebar ... #}
             </div>
        </div>

        {# 3. Hauptinhalt (Bleibt im Layout-Block, damit die CSS-Klasse main-content greift) #}
        <main class="main-content p-4">
            {# Hier wird der Inhalt der jeweiligen Seite eingefügt #}
            {% block body %}{% endblock %}
        </main>

    {% endblock %}
    {# --- ENDE LAYOUT BLOCK --- #}

</body>
</html>