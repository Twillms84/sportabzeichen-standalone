<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Sportabzeichen App{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        
        {# 1. jQuery #}
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

        {# 2. Bootstrap 5 CSS #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        
        {# 3. Bootstrap-Select CSS #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.14.0-beta2/css/bootstrap-select.min.css">

        {# FontAwesome Icons #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

        <style>
            body { min-height: 100vh; overflow-x: hidden; }

            /* --- IServ Style für Dropdowns --- */
            .btn-iserv {
                background-color: #f8f9fa;
                border: 1px solid #ced4da;
                color: #212529;
            }
            .dropdown-menu .active .text { color: #fff !important; }

            /* --- SIDEBAR STYLING (Unverändert) --- */
            .sidebar { background-color: #2c3e50; color: #ecf0f1; min-height: 100vh; }
            .sidebar .nav-link { color: #bdc3c7; padding: 12px 20px; font-size: 1.05rem; border-left: 4px solid transparent; }
            .sidebar .nav-link:hover, .sidebar .nav-link.active { color: #fff; background-color: #34495e; border-left-color: #3498db; }
            .sidebar .nav-link i { width: 25px; text-align: center; margin-right: 10px; }
            .sidebar-brand { padding: 20px; font-size: 1.3rem; font-weight: bold; color: white; background-color: #1a252f; text-decoration: none; display: block; }
            
            .sidebar-wrapper { display: none; }
            @media (min-width: 992px) {
                .sidebar-wrapper { display: block; position: fixed; top: 0; left: 0; bottom: 0; width: 260px; z-index: 100; overflow-y: auto; }
                .main-content { margin-left: 260px; }
                .mobile-header { display: none !important; }
            }
        </style>

    {% block stylesheets %}
        {{ importmap('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ importmap('app') }}
    {% endblock %}
    
    </head>
    <body>

        {# ... Navbar & Sidebar Code (Identisch zu deinem Code) ... #}
        <nav class="navbar navbar-dark bg-dark mobile-header px-3 d-lg-none">
            <button class="btn btn-outline-light" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarMenu">
                <i class="fas fa-bars"></i> Menü
            </button>
            <span class="navbar-brand ms-2">Sportabzeichen</span>
        </nav>

        <div class="offcanvas-lg offcanvas-start sidebar-wrapper bg-dark" tabindex="-1" id="sidebarMenu">
             {# Dein Sidebar Inhalt hier... #}
             <div class="sidebar d-flex flex-column">
                <a href="{{ path('app_exams_dashboard') }}" class="sidebar-brand">
                    <i class="fas fa-medal me-2"></i> Sportabzeichen
                </a>
                {# ... Rest der Sidebar ... #}
                <ul class="nav flex-column mb-auto mt-2">
                     <li class="nav-item"><a href="#" class="nav-link active">Demo Link</a></li>
                </ul>
             </div>
        </div>

        <main class="main-content p-4">
            {# Flash Messages ... #}
            {% block body %}{% endblock %}
        </main>

        {# Bootstrap JS Bundle #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

        
    </body>
</html>