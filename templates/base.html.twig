<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Sportabzeichen App{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    {% block stylesheets %}
    <style>
        :root {
            --osa-bg: #121212;
            --osa-card-bg: #1e1e1e;
            --osa-sidebar: #1a1d21;
            --osa-green: #2c5e32;
            --osa-green-hover: #387840;
            --osa-border: #333;
            --osa-text: #e9ecef;
        }

        body { background-color: var(--osa-bg); color: var(--osa-text); min-height: 100vh; }

        /* Sidebar Desktop */
        @media (min-width: 992px) {
            body { display: flex; }
            .sidebar-wrapper { width: 280px; height: 100vh; position: sticky; top: 0; flex-shrink: 0; border-right: 1px solid var(--osa-border); background-color: var(--osa-sidebar); }
            .main-content { flex-grow: 1; overflow-x: hidden; padding: 2rem; }
        }

        /* Dark Form Elements & Buttons */
        .form-control, .form-select { background-color: #2b3035; border: 1px solid #495057; color: #fff; }
        .form-control:focus { background-color: #33393f; color: #fff; border-color: var(--osa-green); box-shadow: none; }
        
        .btn-osa-primary { background-color: #0d6efd; color: white; border: none; padding: 0.5rem 1.25rem; border-radius: 6px; }
        .btn-osa-success { background-color: var(--osa-green); color: white; border: none; padding: 0.5rem 1.25rem; border-radius: 6px; }
        .btn-osa-success:hover { background-color: var(--osa-green-hover); }

        /* DEINE CUSTOM DROP DOWNS (Harmonisiert) */
        .dropdown-menu-dark-custom {
            background-color: #2b3035 !important;
            border: 1px solid #495057 !important;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            min-width: 250px;
            z-index: 1050;
        }

        .dropdown-item-text { color: #dee2e6; }
        
        /* Fix für die Buttons "Alle" / "Keine" im Dropdown */
        .js-view-all, .js-view-none, .js-group-all, .js-group-none {
            cursor: pointer !important;
            text-decoration: none;
            font-size: 0.85rem;
            padding: 2px 8px;
            border-radius: 4px;
        }
        .js-view-all:hover, .js-group-all:hover { background-color: rgba(44, 94, 50, 0.3); color: #fff; }

        /* Versteckte Spalten (für dein JS) */
        .col-hidden { display: none !important; }
    </style>
    {% endblock %}

    {% block javascripts %}
        {# WICHTIG: jQuery muss vor deinem Script geladen sein, da dein Script 'import $ from jquery' nutzt #}
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        {{ importmap('app') }}
    {% endblock %}
</head>
<body>

    {% block layout %}
        {# 1. MOBILE HEADER #}
        <nav class="navbar navbar-dark bg-dark px-3 d-lg-none sticky-top w-100 border-bottom border-secondary">
            <button class="btn btn-outline-light" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarMenu">
                <i class="fas fa-bars"></i>
            </button>
            <span class="navbar-brand ms-2">Sportabzeichen</span>
        </nav>

        {# 2. SIDEBAR #}
        <div class="offcanvas-lg offcanvas-start sidebar-wrapper bg-dark" tabindex="-1" id="sidebarMenu">
            <div class="sidebar d-flex flex-column h-100">
                <a href="{{ path('app_exams_dashboard') }}" class="sidebar-brand text-decoration-none text-white p-4 d-block">
                     <i class="fas fa-medal me-2 text-warning"></i> <strong>Sportabzeichen</strong>
                </a>
                <hr class="my-0 border-secondary">
                <ul class="nav flex-column mb-auto mt-3 px-3">
                    <li class="nav-item mb-2">
                        <a href="{{ path('app_exams_dashboard') }}" class="nav-link text-white rounded {% if app.request.attributes.get('_route') starts with 'app_exams_' %}bg-success{% endif %}">
                            <i class="fas fa-tachometer-alt me-2"></i> Dashboard
                        </a>
                    </li>
                </ul>
                <div class="mt-auto p-3 border-top border-secondary bg-black bg-opacity-25">
                    <div class="d-flex align-items-center justify-content-between">
                        <span class="small text-truncate me-2">{{ app.user ? app.user.firstname : 'Gast' }}</span>
                        <a href="{{ path('app_logout') }}" class="btn btn-sm btn-outline-danger border-0"><i class="fas fa-sign-out-alt"></i></a>
                    </div>
                </div>
            </div>
        </div>

        {# 3. MAIN CONTENT #}
        <main class="main-content p-4">
            {% block body %}{% endblock %}
        </main>
    {% endblock %}

    <script>
        // Sicherstellen, dass Dropdowns auch nach dynamischen Änderungen funktionieren
        document.addEventListener('DOMContentLoaded', function () {
            var dropdownElementList = [].slice.call(document.querySelectorAll('[data-bs-toggle="dropdown"]'))
            var dropdownList = dropdownElementList.map(function (dropdownToggleEl) {
                return new bootstrap.Dropdown(dropdownToggleEl)
            });
        });
    </script>
</body>
</html>