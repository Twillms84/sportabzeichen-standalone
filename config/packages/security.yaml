security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    role_hierarchy:
        ROLE_ADMIN:       ROLE_EXAMINER
        ROLE_SUPER_ADMIN: [ROLE_ADMIN, ROLE_ALLOWED_TO_SWITCH]

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js|assets|build)/
            security: false
        
        main:
            lazy: true
            provider: app_user_provider
            switch_user: true 
            user_checker: App\Security\UserChecker

            form_login:
                    login_path: app_login
                    check_path: app_login
                    enable_csrf: true
                    username_parameter: email
                    password_parameter: _password
                    default_target_path: app_exams_dashboard
                    # ÄNDERUNG HIER: 
                    # Wir entfernen "use_target_path" und setzen nur "always_...: false"
                    always_use_default_target_path: false
            
            logout:
                path: app_logout
                target: app_home

    access_control:
        # 1. Spezifische Admin-Bereiche
        - { path: ^/admin/user, roles: ROLE_ADMIN }
        - { path: ^/admin/settings, roles: ROLE_ADMIN }
        - { path: ^/admin/participants, roles: [ROLE_EXAMINER] } # ROLE_ADMIN ist durch hierarchy inklusive
        
        # 2. Prüfer-Bereich (Hier liegt vermutlich dein /admin/exams)
        - { path: ^/exams, roles: ROLE_EXAMINER } 

        # 3. Teilnehmer-Bereich (Hier muss 'my_results' liegen!)
        # WICHTIG: Stelle sicher, dass die URL von 'my_results' mit /sportabzeichen beginnt
        - { path: ^/sportabzeichen, roles: ROLE_USER }